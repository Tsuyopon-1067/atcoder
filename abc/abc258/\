#include <stdio.h>
int main() {
    int n, q, x;
    int w[200000];
    int k[200000];
    scanf("%d %d %d", &n, &q, &x);
    for (int i = 0; i < n; ++i) {
        scanf("%d", &w[i]);
    }
    for (int i = 0; i < q; ++i) {
        scanf("%d", &k[i]);
    }
    return 0;
}
