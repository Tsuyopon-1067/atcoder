#include <bits/stdc++.h>
#define _GLIBCXX_DEBUG
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
using namespace std;

int main() {
	int n;
	cin >> n;
	string s1, s2;
	rep(i, n) cin >> s1;
	rep(i, n) cin >> s2;

	int k = 100;
	rep(i, n){
		if(s1[i] == s2[i]){

		}
	}
	return 0;
}
