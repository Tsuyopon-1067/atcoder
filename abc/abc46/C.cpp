#include <bits/stdc++.h>
using namespace std;
#define _GLIBCXX_DEBUG
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
	int n;
	cin >> n;
	int t[1000], a[1000];
	for(int ii = 0; ii < n; ii++) cin >> t[ii] >> a[ii];
	long long tk = 0, ao = 0;

	rep(i, n){
		int s = max((k-1) / t[i], (ta-1) / a[i]) + 1;
	//tt : ta = tk : ao
	//tk = n tt
	//ao = n ta
	}

	return 0;
}
